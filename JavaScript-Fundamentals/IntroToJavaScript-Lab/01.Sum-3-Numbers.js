function sum(input) {
    let firstNum=Number(input[0]);
    let secondNum=Number(input[1]);
    let thirdNum=Number(input[2]);
    let sumNums=firstNum+secondNum+thirdNum;

    console.log(sumNums) ;
}
