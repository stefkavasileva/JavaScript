function printRecAreaAndPerimeter([firstSide,secondSide]) {
    firstSide=Number(firstSide);
    secondSide=Number(secondSide);
    let area=firstSide*secondSide;
    let perimeter=2*(firstSide+secondSide);
    console.log(area);
    console.log(perimeter);
}
